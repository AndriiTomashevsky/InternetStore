@model HeaderViewModel

<header class="@Model.HeaderClass">
	<div class="container">
		<div class="row">
			<div class="col-md-6">
				<h1><i class="@Model.Logo"></i> @Model.Text</h1>
			</div>
		</div>
	</div>
</header>